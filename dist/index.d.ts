import Buzzy from 'buzzy';
export default Buzzy;
export { Config, DefaultConfig, Controller, ControllerFunction, EmptyResult, TextResult, JsonResult, RedirectResult, PageResult, FragmentResult, Helper, Route, DefaultMethod } from 'buzzy';
