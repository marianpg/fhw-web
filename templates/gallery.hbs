{ "template": "template-html" }
---

<h1>{{ page.title }}</h1>
<p>{{ page.description }}</p>

{{#each page.images}}
  <figure>
    <figcaption>{{ title }}</figcaption>
    <img src="{{ src }}" width="400">
  </figure>
{{/each}}
